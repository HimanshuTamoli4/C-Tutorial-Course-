 #include <stdio.h>
int main() {
char s[50];
//gets (s);  // ==> this will strore multiword string 
printf(" Enter your name :");
gets (s);  // ==> this will strore multiword string 
puts (s); // ==> this will lead cursaw to next line 
//printf(" Your name is %s" , s);

return 0;
}